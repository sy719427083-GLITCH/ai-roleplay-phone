# Prototype Instructions

Run the local server yourself and open the preview in the in-app browser. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Approved Work app direction

The Work app is a career simulation inside CCAT OS: read existing worldbooks and linked characters; offer an interactive office, actionable projects, character dialogue, and a separate persistent career save. Keep original worldbook/character records unchanged. Use warm 2D office visuals and readable mobile work tools. The user approved implementation on 2026-09-16.

Work app follow-up: wages settle into the existing Wallet app after a timed project delivery. Countdown persists across reloads and app switches; settlement must be idempotent and survive wallet-history clearing. Keep the Work top bar flat, with no blur or shadow.

Use Apple system sans-serif with tabular numerals for Work clocks/countdowns. Avoid black-translucent PWA status-bar mode: user screenshot shows a native top blur obscuring toolbar text even with CSS shadows disabled. Use the non-translucent default status bar.

Settings release label must read package.json version rather than a hardcoded label. Work countdown expiry should show the exact next prerequisite with a navigation button; distinguish instantaneous workflow checks from the real countdown.

Work v0.3.32 supersedes the manual draft/review workflow: start work, optionally cooperate with a worldbook character, then finish and collect wages after countdown. No required writing, word count, research click, or review gate. Character API responses become persisted opening/support/closing scenes; a successful delegation can shorten the countdown once, and shared work is available to that character in chat. API failures never block salary eligibility. Preserve existing career, wallet receipts, and optional legacy notes.

Work top-edge follow-up: keep document/body/root theme color identical to the Work surface (#f5f4ec). Header spacing must add 12px after the top safe area instead of consuming that margin with max(). CSS checks cannot verify iOS native status-bar effects; do not claim a device-level fix without device evidence.

Work v0.3.34: approved living-office UI uses one user plus up to three linked characters as animated chibi figures, avatar heads with illustrated fallback, clickable people, desk/meeting/coffee destinations and an in-office conversation panel. Keep OfficeScene and one WorkShift mounted across internal tabs to preserve activity and pending requests. Payroll stays exclusively in settle/completePaidWork. Scene animations never authorize wages. Respect prefers-reduced-motion; timer uses Apple system numerals. Office invitations are lightweight scene prompts, not fabricated AI history. Latest AI scene appears in an office speech bubble; full records remain in Work desk.

## Work v0.3.35 — current direction (supersedes earlier Work UI)

The user selected the first white office mock on 2026-09-18 and emphasized neat desks. WorkOffice replaces the old career UI: six employee desks in two aligned columns and three rows, one boss desk, pale wood tea counter at top, white floor/desks, no chairs or rugs. Raster furniture are independently clickable with gentle feedback. Use read-only Me/Character sources and store uploaded/URL avatar overrides only under ccat-office-avatars-v1. No writes to source profiles, worldbooks, wallets or old career saves. Three-dot menu contains Settings; Settings, Project management, Work countdown and Employee management pages are intentionally empty. Bottom countdown button is widest. Work theme is pure white. Release version follows package.json; deploy to existing GitHub Pages via main workflow.

Work v0.3.36 visual correction: user rejected separately regenerated furniture as crooked and unlike the approved preview. Preserve the exact first mock camera, furniture silhouettes, proportions and scene coordinates. Use the retouched approved scene as a shared raster atlas for clickable object slices; crop out its header/footer and render real controls/avatars. Do not approve changed furniture/perspective as minor polish. Compare same-size screenshots to the approved image before release.

Work v0.3.37: user chose circular source avatars moving autonomously through the existing approved office. Preserve the atlas/layout; use walkway routes, coordinated two/three-person chats, exclusive coffee/printer use, return to own desk and live above-head activity labels. This is local ambient behavior, without API calls, fabricated dialogue history or source/save mutations. Pause while an internal page, menu or avatar editor is open and when document is hidden; respect reduced motion. Keep the four placeholder pages empty.

Work v0.3.38: center each seated circular avatar over its own desk (horizontal desk midpoint), with an approach/exit at the desk front. User approved richer weighted random work, collaboration, office chores, rest and occasional TV/Douyin/game leisure. Randomize initial scene state and subsequent activities/durations; avoid immediate repeat desk activity. Preserve the scene atlas and no-API/source-isolation boundaries. Leisure has distinct icons and subtle reduced-motion-safe feedback.

Work v0.3.39: keep home avatars centered horizontally but lower at the desk front so monitors remain visible. Settings now supports explicit local versus main-API generated dialogue. Show actual generated speech per speaker and a full current-conversation transcript; label local presets, never fake an AI fallback. Send only participating source character/world context, office roles and assigned tasks. Defer/abort stale requests, cap automatic requests to one per minute, hold groups through playback and offer retry/dismiss after errors. Employee management now supports employee/supervisor positions, direct reports, queued task assignment and entry points to the existing local-photo/URL avatar editor. Store team settings only under ccat-office-team-v1; source records remain untouched. Project/countdown remain placeholders.

Work v0.3.40: randomly mix shared-floor chats with two-person desk visits for collaboration, questions and casual talk. The host remains at their own desk while the visitor follows the aisle to a neighboring front-of-desk spot. Dialogue starts only on arrival and reuses AI/local playback and hold logic. On completion/cancellation, the host resumes work in place and only the visitor walks home. Preserve centered desk-front avatars and existing office artwork.

Work v0.3.41: Project management now displays five randomized distinct local projects with name, reward, estimated minutes and detailed brief. Top-right refresh has five free uses per local calendar day (assumed daily pending optional user clarification), then costs 200 from the existing Wallet. Initial generation consumes no quota; persist board and counts under ccat-office-projects-v1. Paid refresh journals its next board and uses durable wallet expense receipts so interrupted saves recover without duplicate charges; errors require read/recovery before more refreshes. Insufficient balance retains the current list. This authorized Wallet debit supersedes the earlier no-wallet-write boundary only for refresh expenses. No project acceptance/countdown/payroll scope in this increment.

Work v0.3.42: User authorized accepting projects, max three per local calendar day, independent real-time concurrent deadlines, center countdown synchronization and automatic Wallet payment. New ledger ccat-office-jobs-v1 persists immutable accepted snapshots and paid status. All offers pay 40 per real minute (longer duration strictly pays more); existing unaccepted offers migrate, accepted rewards stay unchanged. Completion never restores same-day allowance and refresh never removes accepted work. A root-level provider settles while any app/lockscreen is mounted and catches up after closed-browser elapsed time on reopening. Income receipts prevent duplicate wages after interrupted saves/history clearing. All live Wallet writers (payroll, board refresh, manual wallet edits/history, chat transfers) share withWalletLock; visible Wallet subscribes to updates and must never auto-write stale component state. Office art/activity remains unchanged.

Work v0.3.43: User requires only one active project at a time, improved project typography/layout, and API-generated varied projects. Preserve already-accepted legacy parallel jobs and block new acceptance until all finish; still max3 accepts/localday. Replace local catalog with selected main API generation. First AI batch is free; each successful subsequent batch counts/charges under existing rules, with no local fallback. Validate exactly five complete projects, 15–120 integer minutes, client-computed40/min rewards, and recent title/content similarity (50 retained briefs). Retry malformed/repeated result once; abort on leaving page and time out after60s. Snapshot/commit use shared wallet lock, network does not; batch comparison rejects cross-tab stale commits. White layout has compact quota strip, active-work shortcut, reward/time hierarchy and expandable project details. No API secrets or original character/worldbook records in prompts beyond the selected endpoint authorization; prompt contains recent project briefs only.

Work v0.3.44: Above-avatar activity text must show concrete activity only, without category/assignment prefixes or participant/location suffixes. Examples: 刷抖音, 玩一局游戏, 核对数据, 闲聊. Keep full activity metadata for AI context. Travel/wait/return labels remain accurate but brief (前往茶水吧/等待同事/返回工位).

Work v0.3.45: Employee management explicitly selects a Me/Character source for every seat, including boss. Unselected/missing sources keep furniture but render no person and never simulate or chat. Preserve explicit saved selections; do not auto-fill empty seats. Avatar overrides remain Work-only; restoring source avatar retains selection. Reconcile roster immediately, end affected conversations and resolve absent supervisors to boss without rewriting saved team assignments. Cover the full office content area with the approved atlas at a uniform scale, centered with small aspect-ratio crops; never stretch furniture or reintroduce side gutters.

Work v0.3.46 supersedes v0.3.45 cover sizing: user rejected cropped corners. Always fit the entire 853:1606 office scene inside the content area at uniform scale; never enlarge to cover or stretch furniture. Extend only the surrounding pale wall/floor background with subtle daylight gradients. Keep all furniture, scene coordinates, actors and click targets together inside the complete scene.

Work v0.3.47: approved first immersion increment is employee-initiated choice events and subsequent memory. Six local event types offer three choices; label these as local events. Reserve boss-front report destination for visiting employee until response; preserve active event/cooldown and last 60 experiences in ccat-office-events-v1. Mood/trust are character-source-key scoped and feed AI context with latest five own memories; local speakers can recall their own experience. Keep original characters/worldbooks, project timing and wallet unchanged. One pending event, initial 8s simulation grace and 2-minute real cooldown after response.
