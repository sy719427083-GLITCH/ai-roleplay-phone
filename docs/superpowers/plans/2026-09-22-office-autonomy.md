# Autonomous office stories

Approved: observer mode replaces employee-to-player choice events. Characters converse, choose and execute actions without waiting for the user. Keep complete office atlas and wallet/project mechanics.

- Reuse existing autonomous rendezvous and chat dispatch, with local rule or validated AI action coffee/cooperate/work after dialogue playback.
- Route every participant to separate valid anchors; reserve coffee station; if busy return to work with explicit reason. Hold action until everyone arrives, then return home.
- Persist bounded independent story log, participant identities, original dialogue, decision and completion/interruption state. Expose only participant memories to AI.
- Replace choices with live observation and log; person click observes, avatars remain editable in employee management. Remove manual task forms. Observe/transcript overlays do not pause life.
- Verify state transitions, new routes, decision validation and memory isolation; mobile real-component fixture on separate localhost origin; bump, build, tests and existing Pages deployment.
